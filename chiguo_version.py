# 迟菓项目版本号（单一来源）
#
# 版本格式：MAJOR.MINOR 字符串（如 "1.10"）。版本号是标识符，不是数值——
# 禁止按十进制加法解读（1.9 + 0.1 = 2.0 是错误推导）。
#
# 步进规则（维护者手动，每完成一轮功能修改执行一次）：
#   - MINOR + 1：1.9 → 1.10 → 1.11 → 1.12 …（次版本计数递增，类比 Linux 内核 6.9 → 6.10）
#   - MAJOR 保持 1；仅在重大架构变更或维护者明确决定时 +1（届时 1.xx → 2.0）
#
# 版本号只在本文件修改，其他模块 import 引用（daemon --version / envcheck / monitor / decision JSON）。
VERSION = "1.10"
